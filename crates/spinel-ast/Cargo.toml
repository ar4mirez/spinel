[package]
name = "spinel-ast"
description = "Spinel's own Ruby AST: the only tree anything outside spinel-parse sees."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]


[lints]
workspace = true
