[package]
name = "spinel-regex"
description = "Ruby's regex dialect: parser and backtracking matcher, from scratch in Rust."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[lints]
workspace = true

# `bench/` at the repo root, per CLAUDE.md, for the reason `spinel-vm` gives:
# the benchmarks describe the engine rather than one crate, and this one is
# compared against system `ruby --yjit`.
[[bench]]
name = "regex_captures"
path = "../../bench/regex_captures.rs"
harness = false
