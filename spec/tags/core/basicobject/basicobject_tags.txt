fails(A bare constant inside `BasicObject` must not resolve through `Object`, because `BasicObject` is not below it. Spinel resolves every bare constant the same way whatever the cref is, so the reference succeeds where Ruby raises NameError. Tracked by #185. Became visible rather than blocked when #183 taught the harness to load ruby/spec fixtures):BasicObject raises NameError when referencing built-in constants
fails(The same missing rule as the NameError example above, seen through `defined?`: it answers "constant" where Ruby answers nil, because the lookup it asks about does not know `BasicObject` has an empty namespace. Tracked by #185. Became visible rather than blocked when #183 taught the harness to load ruby/spec fixtures):BasicObject does not define built-in constants (according to defined?)
