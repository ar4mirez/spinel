fails(Measures the arity of the block Enumerable hands to `each`, which CRuby forwards from the caller's block. A core library written in Ruby cannot forward arity: the `each { |*values| }` block this module iterates with reports -1 whatever the caller wrote. Reachable only by making the arity of a Ruby block settable, or by writing `map` as a primitive; neither is worth the trade today):Enumerable#map reports the same arity as the given block
