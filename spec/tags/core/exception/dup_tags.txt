fails(#201: `dup` copies the cell, the shape and the ivar storage and then stops, so `initialize_copy` never runs. The fixture defines one that records `object_id`, which is the whole example. Calling it needs a frame push, the way `Class#new` pushes one for `initialize`. Became visible rather than blocked when class variables let `core/kernel/fixtures/classes.rb` compile):Exception#dup calls #initialize_copy on the new instance
fails(#201: the same hook. The ivars this asks for are set by the fixture's `initialize_copy`, not by the original, so a `dup` that copies ivars faithfully still answers nil here):Exception#dup copies instance variables
