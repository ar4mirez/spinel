fails(`4.2 <=> obj` must call `coerce` on obj and raise TypeError "coerce must return [x, y]" when it answers something other than a two-element array. `core/float.rb` answers nil for anything that is not a `Numeric`, which is right for an object with no `coerce` and wrong for one with a bad one. The fix is the coercion protocol — `Numeric#coerce`, and the `coerce`-then-retry path in every numeric operator — not a special case in `<=>` — that is #179. Became visible rather than blocked when #162 landed anonymous classes: the example builds its misbehaving object with `Class.new`):Float#<=> raises TypeError when #coerce misbehaves
