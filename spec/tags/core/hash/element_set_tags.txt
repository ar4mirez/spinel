fails(Ruby copies and freezes a String key on the way into a Hash, so mutating the original afterwards does not change the key. Spinel stores the object it was handed: there is no `String#dup` and no frozen bit to set. Needs both, so it is #19's slice for the String half and #22's for the Hash half. Became visible rather than blocked when #157 landed hash literals):Hash#[]= duplicates string keys using dup semantics
