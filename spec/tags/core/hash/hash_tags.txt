fails(Spinel has no structural `#hash`: `Object#hash` answers something per-object, so two equal values hash differently and a recursive structure has no cycle rule at all. Ruby's answer combines the elements' own hashes and gives a recursive one a fixed stand-in, which needs a hashing primitive this VM does not have yet. Became visible rather than blocked when #157 landed hash and range literals, which is what lets the example build the values it compares. Tracked by #22):Hash#hash returns a value which doesn't depend on the hash order
fails(Spinel has no structural `#hash`: `Object#hash` answers something per-object, so two equal values hash differently and a recursive structure has no cycle rule at all. Ruby's answer combines the elements' own hashes and gives a recursive one a fixed stand-in, which needs a hashing primitive this VM does not have yet. Became visible rather than blocked when #157 landed hash and range literals, which is what lets the example build the values it compares. Tracked by #22):Hash#hash returns the same hash for recursive hashes
fails(Spinel has no structural `#hash`: `Object#hash` answers something per-object, so two equal values hash differently and a recursive structure has no cycle rule at all. Ruby's answer combines the elements' own hashes and gives a recursive one a fixed stand-in, which needs a hashing primitive this VM does not have yet. Became visible rather than blocked when #157 landed hash and range literals, which is what lets the example build the values it compares. Tracked by #22):Hash#hash returns the same hash for recursive hashes through arrays
