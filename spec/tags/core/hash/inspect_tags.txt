fails(Two gaps, and the Hash one cannot be fixed first. `Symbol#inspect` in Spinel never quotes, so `:"needs-quotes"` inspects as `:needs-quotes`, and Ruby 3.4's Hash format writes a Symbol key as `key: value` reusing exactly that quoting. Fixing `Hash#inspect` alone would print `needs-quotes: 1` unquoted, which is a different wrong answer. #20 owns the symbol-literal rule and #22 owns the Hash format. Became visible rather than blocked when #157 landed hash literals):Hash#inspect adds quotes to symbol keys that are not valid symbol literals
