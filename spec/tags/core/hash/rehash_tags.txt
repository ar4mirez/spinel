fails(`rehash` re-files every key after one of them mutated, which only means anything once lookup is by hash code. `core/hash.rb` is still an association list walked with `eql?`, so a mutated key is found again without any rehashing and the example's `include?` answers true where Ruby answers false. The open-addressed table is #22. Became visible rather than blocked when #157 landed hash literals):Hash#rehash reorganizes the Hash by recomputing all key hash codes
