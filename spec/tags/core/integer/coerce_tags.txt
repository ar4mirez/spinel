fails(`1.coerce` on a non-number String must raise ArgumentError, because `Numeric#coerce` answers a pair of Floats and Ruby's `Float` conversion parses a String strictly rather than leniently. Spinel has neither `Kernel#Float` nor `String#to_f`, so `core/numeric.rb` stands in with `to_f` — right for every type the VM has today, and wrong for String. Adding `String#to_f` alone would not close it: `to_f` on a non-number String answers 0.0 where Ruby's `Float` conversion raises, so the two are different functions and #181 has to write both. Became visible rather than blocked when #179 landed `Numeric#coerce`, which is what makes the example run at all):Integer#coerce fixnum when given a String raises an ArgumentError when trying to coerce with a non-number String
