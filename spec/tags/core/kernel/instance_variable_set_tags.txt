fails(#207: a name that converts through `to_str` but is not a valid ivar name is Ruby's NameError; this answers TypeError, which is the class reserved for an argument that does not convert at all):Kernel#instance_variable_set raises a NameError if the passed object, when coerced with to_str, does not start with @
