fails(#202: `is_a?` is Ruby on top of `self.class.ancestors`, and `Object#class` skips singleton classes by design — so a module the object was `extend`ed with is not in the chain this asks. Needs a primitive for the real, singleton-inclusive ancestry):Kernel#is_a? returns true if given a Module that object has been extended with
fails(#202: the same `self.class`, from the other side: `class` is an ordinary method a program can override, and asking it means believing the override. Ruby reads the class out of the object header):Kernel#is_a? does not take into account `class` method overriding
