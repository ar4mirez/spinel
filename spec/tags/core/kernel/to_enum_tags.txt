fails(`to_enum` and `enum_for` answer an `Enumerator`, which needs fibers — phase 2, tracked by #26 and #16. #15 gave `respond_to?` a real answer, which turned this example from blocked into a failure: it asks `respond_to?` for `:to_enum` including private methods, and Spinel now says false honestly rather than being unable to say anything. Defining a `to_enum` that answered something other than an Enumerator would be a wrong answer):Kernel#to_enum is defined in Kernel
