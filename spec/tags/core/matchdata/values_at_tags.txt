fails(`MatchData#values_at` takes Integer indices only. A Range argument now reaches it as a real object rather than an unbuildable literal, and it raises TypeError where Ruby inspects the Range's bounds and raises RangeError. Range-aware indexing across the core classes is #33's slice for MatchData. Became visible rather than blocked when #157 landed range literals):MatchData#values_at when passed an integer Range raises RangeError if any element of the range is negative and out of range
