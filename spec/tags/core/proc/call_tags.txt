fails(Spinel has no frozen bit, so `# frozen_string_literal: true` is parsed and recorded on the literal and then not honoured: every evaluation of a string literal allocates a new String and `equal?` is false where Ruby says true. Freezing is #19's slice. Became visible rather than blocked when #183 taught the harness to load the fixture this file requires):Proc#call on a Proc created with frozen_string_literal: true/false doesn't duplicate frozen strings
