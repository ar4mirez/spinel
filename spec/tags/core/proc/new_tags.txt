fails(A subclass of Proc has no built-in class id of its own, so `Class#allocate` gives it the plain-object shape and `ProcSubclass.new` answers an object where Ruby raises ArgumentError. `Proc.new` itself refuses correctly; it is only the subclass that slips past. Routing such a class to its built-in ancestor's allocation arm was measured and costs 45 examples elsewhere — a Hash subclass loses its own class and its `default` with it — so the representation a subclass of a built-in gets is its own slice. Became visible rather than blocked when #193 let `core/proc/fixtures/common.rb` compile):Proc.new without a block raises an ArgumentError if invoked on a subclass from within a method with no block
