fails(The first half passes: `"a" =~ "b"` raises TypeError. The second builds `StringSpecs::MyString.new "b"`, and a subclass of String cannot hold bytes yet — `Class#allocate` gives it the plain-object shape, so even `inspect` on one refuses. That is the String-subclass representation, not `=~`. Became visible rather than blocked when #187 let the fixture file compile):String#=~ raises a TypeError if a obj is a string
