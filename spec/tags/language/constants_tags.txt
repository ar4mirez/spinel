fails(`private_constant` is accepted and does nothing, so reopening the constant succeeds where Ruby raises NameError. Constant visibility is tracked by #185. Became visible rather than blocked when #183 taught the harness to load ruby/spec fixtures):Module#private_constant marked constants in a module cannot be reopened as a module from scope where constant would be private
fails(The same missing `private_constant` enforcement as the module form above, reopening the constant as a class instead. Tracked by #185. Became visible rather than blocked when #183 taught the harness to load ruby/spec fixtures):Module#private_constant marked constants in a module cannot be reopened as a class from scope where constant would be private
