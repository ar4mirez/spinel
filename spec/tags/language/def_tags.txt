fails(A `def` nested inside another `def` is public even where the outer one was made private, and Spinel keeps no visibility in the method table at all, so calling it with an explicit receiver succeeds where Ruby raises NoMethodError. Method visibility is #161. Became visible rather than blocked when #183 taught the harness to load ruby/spec fixtures):A method definition always resets the visibility to public for nested definitions at the toplevel
