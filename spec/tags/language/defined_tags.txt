fails(`defined?` of a private method reached through a module receiver must answer nil, and Spinel's method table has no notion of public or private yet. #13 named this when it landed `defined?`; the visibility slice #161 is what closes it. Answering "method" here is a wrong answer rather than a missing one, so the example is skipped instead of left to fail):The defined? keyword when called with a method name having a module as receiver returns nil if the method is private
