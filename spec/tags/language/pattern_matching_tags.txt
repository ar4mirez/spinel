fails(A hash pattern that fails on a *missing key* raises NoMatchingPatternKeyError rather than the general error, and only when it is the sole clause: measured on ruby 4.0.6, `case {a: 1}; in {a: 2}; in {b: 2}; end` raises the general NoMatchingPatternError. Spinel's `case`/`in` decides which error to raise where nothing matched, and by then which clause failed on what is gone. Reporting it needs the failure reason to travel out of the pattern, which is more machinery than the one example is worth; #165 raises NoMatchingPatternError for both):Pattern matching Hash pattern raises NoMatchingPatternKeyError if the key does not match
