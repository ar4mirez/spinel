fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):Global variable $-d is an alias of $DEBUG
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Execution variable $: is initialized to an array of strings
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Execution variable $: is read-only
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Global variable $" is read-only
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Global variable $-a is read-only
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Global variable $-l is read-only
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Global variable $-p is read-only
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Global variable $0 raises a TypeError when not given an object that can be coerced to a String
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Global variable $< is read-only
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Global variable $? is read-only
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Global variable $FILENAME is read-only
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Global variable $VERBOSE is false by default
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $! is read-only
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $, raises TypeError if assigned a non-String
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $-0 raises a TypeError if assigned a boolean
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $-0 raises a TypeError if assigned an Integer
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $. can be assigned a Float
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $/ raises a TypeError if assigned a boolean
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $/ raises a TypeError if assigned an Integer
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $@ cannot be assigned when there is no a rescued exception
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $\ raises a TypeError if assigned not String
fails(A predefined global with an assignment rule of its own. #166 gave the language a global table; a name the VM owns — read-only, type-checked, or initialised by the runtime — needs the runtime that owns it, which is #39. Became visible rather than blocked when assignment started compiling):Predefined global $stdout raises TypeError error if assigned to nil
