fails(`[[:blank:]]` must match U+1680 OGHAM SPACE MARK. spinel-regex's POSIX brackets are the ASCII sets plus the Unicode properties #14 measured, and the blank set is one of the ones still ASCII-only. This became visible rather than blocked when `NilClass#to_a` arrived in #15: the match returns nil and `nil.to_a` is now `[]`. Widening the class belongs with the rest of the dialect work, next to KNOWN_DIVERGENCES in crates/spinel-regex/tests/oracle.rs):Regexp with character classes matches Unicode space characters with [[:blank:]]
