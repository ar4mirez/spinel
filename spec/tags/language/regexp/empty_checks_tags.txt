fails(needs Onigmo's capture-restore records: a capture set by a loop iteration survives backtracking out of that iteration, but a capture set outside a loop does not. spinel-regex snapshots captures at every backtrack point, so it restores in both places. Nineteen of this example's twenty assertions already hold; the twentieth needs the restore-record rewrite, not a wider engine, and is #177. See KNOWN_DIVERGENCES in crates/spinel-regex/tests/oracle.rs):empty checks in Regexps shouldn't cause the Regexp parser to get stuck in a loop
