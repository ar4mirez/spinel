fails(a call carrying both a splat and a block argument must expand the splat before the block argument is evaluated. Spinel expands splats in the call itself, after every argument including the block is on the stack, so `args.pop` runs first and the splat sees the shortened array. Fixing it means a dynamic argument count at the call site — the count is static on `Insn::Send` today — which is #160, not the core library's. Became visible when `Array#pop` arrived in #15):Invoking a method expands the Array elements from the splat before applying block argument operations
fails(the same argument-evaluation order as the other tag in this file, with a positional argument after the splat. One fix, #160, closes both):Invoking a method evaluates the splatted arguments before the block if there are other arguments after the splat
