fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $ARGV to $*
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $DEFAULT_INPUT to $<
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $DEFAULT_OUTPUT to $>
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $INPUT_LINE_NUMBER to $.
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $INPUT_RECORD_SEPARATOR to $/
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $LAST_MATCH_INFO to $~
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $LAST_PAREN_MATCH to $+
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $MATCH to $&
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $NR to $.
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $PID to $$
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $POSTMATCH to $'
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $PREMATCH to $`
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $PROCESS_ID to $$
fails(A global alias. #166 gave the language a global table, in which two names are two entries; making one name another's second spelling is #39's, along with the runtime state most of these alias. Became visible rather than blocked when assignment started compiling):English aliases $RS to $/
