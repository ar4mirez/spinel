fails(Needs the C-API, which Spinel does not have and will not: extensions are the `spinel-ext` Rust API or FFI, per the fixed decisions in CLAUDE.md. The whole `optional/capi` tree measures a compiled extension calling `rb_define_class`, so this example cannot be made to pass rather than being waiting on a slice. Phase 5 decides what to do with the tree):C-API Class function a constant defined in C raises TypeError if constant given as class name exists and is a Number
